const state={products:[]};async function load(){const r=await fetch('products.json');state.products=await r.json();renderFeatured();renderShop()}function el(h){const t=document.createElement('template');t.innerHTML=h.trim();return t.content.firstElementChild}function productCard(p){return el(`<div class="card"><img src="${p.image}" alt="${p.name}"><div class="body"><h4>${p.name}</h4><div class="price">Rs. ${p.price.toLocaleString()}</div></div></div>`)}function renderFeatured(){const f=document.querySelector('#featured');if(!f) return;f.innerHTML='';state.products.slice(0,6).forEach(p=>f.appendChild(productCard(p)))}function renderShop(){const s=document.querySelector('#shopGrid');if(!s) return;s.innerHTML='';state.products.forEach(p=>s.appendChild(productCard(p)))}document.addEventListener('DOMContentLoaded', load);